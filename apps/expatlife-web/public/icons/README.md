# PWA Icons

Add the following PNG files for installable PWA and Apple touch icon support:

- `icon-192.png` — 192×192 px
- `icon-512.png` — 512×512 px

You can generate these from `app/icon.svg` using a tool like ImageMagick, Sharp, or an online converter. The manifest references these paths.
