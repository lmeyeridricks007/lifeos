/**
 * Shared vertical rhythm for guide / city key columns (aligns with moving pillar spacing intent).
 */
export const guideKeyColumnStackGapClass = "gap-6 sm:gap-7 md:gap-8";

/** Outer padding wrapper for city key sections grid (main + TOC). */
export const cityGuideKeySectionsPadClass = "py-2 md:py-4";
