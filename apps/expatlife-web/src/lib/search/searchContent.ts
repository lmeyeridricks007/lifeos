/**
 * @deprecated Prefer importing `runSearch` from `@/src/lib/search/runSearch` and types from `searchDocument`.
 * Kept as a stable alias for older imports.
 */

export { runSearch as searchContent } from "./runSearch";
export type { SearchResult as SearchResultItem } from "./searchDocument";
