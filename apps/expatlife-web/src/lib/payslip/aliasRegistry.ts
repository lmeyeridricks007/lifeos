/**
 * Alias registry entry point — canonical terms and sorted alias helpers live in `canonicalRegistry.ts`.
 */
export { CANONICAL_PAYROLL_TERMS, sortedAliasesForTerm, type CanonicalPayrollTerm, type RowShapeHint } from "./canonicalRegistry";
