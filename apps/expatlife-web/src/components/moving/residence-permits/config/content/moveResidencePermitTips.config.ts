import type { MoveVisaResidencyTip } from "../../../visas-residency/config/moveVisaResidency.types";

/** Practical tip callouts (reassurance band, etc.). */
export const moveResidencePermitTips: MoveVisaResidencyTip[] = [
  {
    id: "calm",
    title: "You can get the big picture without knowing every rule",
    body: "This page explains **how permits fit together**, **what to watch over time**, and **where to click next** on ExpatCopilot. For anything that decides your stay, use **IND.nl** or a **qualified adviser**—not a single blog post.",
  },
];
