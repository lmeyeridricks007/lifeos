import { statusChangesPageMeta } from "./config/statusChanges.config";

const meta = statusChangesPageMeta;

export const STATUS_CHANGES_CANONICAL = meta.canonicalPath;
export const PAGE_HERO_SUBTITLE = meta.hero.subtitle;
