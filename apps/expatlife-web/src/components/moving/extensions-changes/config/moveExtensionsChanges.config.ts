/**
 * Extensions & Changes — composed page meta.
 * Content lives in slice files (`moveExtensionsChanges*.config.ts`) and is assembled in `moveExtensionsChanges.assemble.ts`.
 */
export { moveExtensionsChangesPageMeta } from "./moveExtensionsChanges.assemble";
export { moveExtensionsChangesRoutes } from "./moveExtensionsChanges.routes";
