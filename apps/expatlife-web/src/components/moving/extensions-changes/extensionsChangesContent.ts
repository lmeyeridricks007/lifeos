import { moveExtensionsChangesPageMeta } from "./config";

const meta = moveExtensionsChangesPageMeta;

export const EXTENSIONS_CHANGES_CANONICAL = meta.canonicalPath;
export const PAGE_HERO_SUBTITLE = meta.hero.subtitle;
